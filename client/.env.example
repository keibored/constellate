# Leave blank to connect through the page origin (Vite proxies Socket.IO in development).
VITE_SERVER_URL=
# Development only. Match the backend's PORT if you change it.
SERVER_PROXY_TARGET=http://127.0.0.1:3000
