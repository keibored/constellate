# Backend-only production baseline. The React frontend is deployed once on Vercel.
# The Vercel production origin is prompted; datastore credentials stay server-only.
services:
  - type: web
    name: constellate-api
    runtime: node
    region: singapore
    plan: free
    numInstances: 1
    buildCommand: npm ci --include=dev && npm run build --workspace=server
    startCommand: npm run db:migrate:production && node server/dist/index.js
    healthCheckPath: /api/health
    autoDeployTrigger: "off"
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 24.16.0
      - key: HOST
        value: 0.0.0.0
      - key: CLIENT_ORIGINS
        sync: false
      - key: TRUST_PROXY_HOPS
        value: "1"
      - key: LOG_LEVEL
        value: info
      - key: HEALTH_TIMEOUT_MS
        value: "2000"
      - key: SHUTDOWN_TIMEOUT_MS
        value: "25000"
      - key: DATABASE_URL
        fromDatabase:
          name: constellate-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: constellate-redis
          property: connectionString
      - key: REDIS_KEY_PREFIX
        value: constellate:production

  - type: keyvalue
    name: constellate-redis
    region: singapore
    plan: free
    maxmemoryPolicy: noeviction
    persistenceMode: off
    ipAllowList: []

databases:
  - name: constellate-postgres
    region: singapore
    plan: free
    postgresMajorVersion: "18"
    ipAllowList: []
